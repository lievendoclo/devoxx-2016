spring:
  application:
    name: zuulproxy
  cloud:
    config:
      discovery:
        enabled: true
    zookeeper:
      connect-string: ${ZOOKEEPER_HOSTNAME:localhost}:2181
      discovery:
        instance-port: ${server.port}

server:
  port: 8081

endpoints:
  restart:
    enabled: true