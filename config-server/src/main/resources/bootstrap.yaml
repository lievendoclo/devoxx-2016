spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lievendoclo/config-repo-test

eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_HOSTNAME:localhost}:8761/eureka/

server:
  port: 8888