spring:
  application:
    name: service-two
  cloud:
    config:
      discovery:
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_HOSTNAME:localhost}:8761/eureka/

server:
  port: 8083
